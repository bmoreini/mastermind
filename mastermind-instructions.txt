/* Mastermind I - no saved turns */

/* Initialization */
//colors, code, guess, feedback, cleanFeedback -- all arrays -- and turn
// colors = r,b,g,w,c,y

/* Main Function */
// define Main function
	// call setCode function
	// start while loop - check if last cleanFeedback = b, or first guess != q
		// increment turn
		// set guess = getGuess
		// set feedback = testGuess(guess)
		// set cleanFeedback = formatFeedback(feedback)
		// alert turn and cleanFeedback
	// }
	// alert "Charlie you've won" if while loop ended with first condition
	// alert "Quitter!" if while loop ended with section condition
//}

/* Functions */

/* Create the Secret Code */
// define function setCode to pull from six colors to randomly fill code with four values 0-5

/* Get a Player's Guess */
// define function getGuess to prompt player for each of four values and stores in guess array

/* Analyze the Guess */
// define function testGuess to analyze guess against code and produces feedback
  // initialize the temporary code and guess arrays as copies
	// count the blacks and erase tempcode and tempguess as you go - one loop
	// count the whites and erase tempcode and tempguess as you go - nested loops
        // use "continue" once a match is found in the inner loop
	// console log the blacks feedback
	// return the feedback


/* Format the Feedback */
// define function formatFeedback to write cleanFeedback to put b's first, w's second, delete the rest
	// initialize the black and white count variables
	// count the blacks and whites in a loop
	// define remainder as 4 - (b + w)
	// write the black pegs first to cleanFeedback
	// write the white pegs second to cleanFeedback
	// delete the blanks in the remainder with array.pop
	// console log the formatted cleanFeedback
	// return the new feedback array

